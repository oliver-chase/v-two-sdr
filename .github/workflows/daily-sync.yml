name: SDR Daily Sync

on:
  schedule:
    - cron: '0 12 * * 1-5'  # 7:00 AM ET (UTC-5)
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "hello from daily-sync"
